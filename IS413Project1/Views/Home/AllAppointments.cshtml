@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<!--View Appointment Page: This page will list out all of the appointments that have been made-->
@*@model IEnumerable<JoinDataViewModel>*@
<!--SignUpPage: This will display the available times. Once they select a time they are routed to a form that has the date info set-->
<!--This is an example of you can display all of the seed data-->
@*@foreach (var item in Model)
    {
        <div class="card card-outline-primary m-1 p-1">
            <div class=“bg-faded p-1">
                <h3>@item.appointmentVm.BeginTime</h3>

                <p>@item.signupVm.GroupName</p>
            </div>
        </div>*@
}
@model IEnumerable<IS413Project1.Models.Signup>
<!--SignUpPage: This will display the available times. Once they select a time they are routed to a form that has the date info set-->
<!--This is an example of you can display all of the seed data-->


@foreach (var x in Model)
{
    @*<div class="card card-outline-primary m-1 p-1">
            <div class=“bg-faded p-1">
                <form method="post" asp-action="AvailAppointments">
                    <input type="hidden" name="appointmentId" value="@x.AppointmentId" />
                    <h3>@x.BeginTime</h3>
                    <p>@x.Description</p>
                    <button class="btn-primary" type="submit">Book Appointment</button>
                </form>
            </div>
        </div>*@

    <div class="container text-left">
        <div class="panel-group">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h4 class="panel-title p-3">
                        <a data-toggle="collapse">@x.BeginTime:@x.GroupName</a>
                    </h4>
                </div>
                <div id="@x.BeginTime" class="collapse">
                    <div class="panel-body">
                        <p>Start Time: @x.GroupName</p>
                        @*<p>Description: @x.Description</p>*@
                    </div>
                </div>
            </div>
        </div>
    </div>

}
